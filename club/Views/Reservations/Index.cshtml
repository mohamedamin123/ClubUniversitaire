@model IEnumerable<club.Models.Reservation>

@{
    ViewData["Title"] = "Index";
}

<h1>Les Reservations</h1>

<p>
    <a asp-action="Create" class="btn btn-warning">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.date)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.timeDeb)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.timeFin)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.reservedBy)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.clubName)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.date)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.timeDeb)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.timeFin)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.reservedBy)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.clubName)
            </td>
            <td>
                    <a asp-action="Edit" class="btn btn-info" asp-route-id="@item.Id">Edit</a> |
                    <a asp-action="Details" class="btn btn-success" asp-route-id="@item.Id">Details</a> |
                    <a asp-action="Delete" class="btn btn-danger" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
